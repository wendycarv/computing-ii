#ifndef STATUS_H
#define STATUS_H

enum status { FAILURE, SUCCESS };
typedef enum status Status;

//or typedef enum status { FAILURE, SUCCESS } Status;

enum boolean { FALSE, TRUE };
typedef enum boolean Boolean;

#endif
